Convertir a mayúsculas una cadena

#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main(){
    string texto;
    getline(cin, texto);

    transform(texto.begin(), texto.end(), texto.begin(), ::toupper);

    cout << texto;
    return 0;
}
