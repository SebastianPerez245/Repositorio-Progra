2. Contar cuántas palabras hay en una cadena

#include <iostream>
#include <string>
#include <sstream>
using namespace std;

int main(){
    string texto;
    getline(cin, texto);

    stringstream ss(texto);
    string palabra;
    int contador = 0;

    while(ss >> palabra) contador++;

    cout << contador << " palabras";
    return 0;
}
